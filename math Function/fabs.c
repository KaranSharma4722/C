//it will return the absolute inputed variable..
#include<stdio.h>
#include<math.h>
int main(){
    double n;
    printf("Enter one number:");
    scanf("%lf",&n);
    printf("ans is:%.1lf",fabs(n));
    return 0;
}
